DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="change-this-to-a-secure-random-string"
NEXTAUTH_URL="http://localhost:3000"
WEBHOOK_SECRET="your-tradingview-webhook-secret"
POLYMARKET_API_URL="https://clob.polymarket.com"
POLYMARKET_API_KEY=""
REDIS_URL="redis://localhost:6379"
ENCRYPTION_KEY="32-char-encryption-key-change-me!"
COMMISSION_RATE="0.02"

# Public base URL of the deployment — used for canonical URLs, OG tags, email links,
# and on-ramp return URLs. MUST be set in production (e.g. https://copytradesplus.com).
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# On-ramp webhook signing secrets — required to verify provider callbacks before crediting.
ONRAMP_WEBHOOK_SECRET=""
COINBASE_WEBHOOK_SECRET=""
